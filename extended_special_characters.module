<?php
/**
 * @file
 * General functions and hook implementations.
 *
 *
 */

function extended_special_characters_add_extended_special_character_options(&$data) {
  $default_options = ['🚀', '👽', '🌟', '😀', '🤯', '🤘','🔥'];
  $data['options'] = array_merge($data['options'],$default_options);
}
